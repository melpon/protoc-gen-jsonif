syntax = "proto3";

package repeated;

enum Enum {
    FOO = 0;
    BAR = 1;
}

message Message {
    string name = 1;
}
message Test {
    repeated int32 a = 1;
    repeated string b = 2;
    repeated Enum c = 3;
    repeated Message d = 4;
}